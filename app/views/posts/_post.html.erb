<% @posts.each do |post| %>
  <tr>
  	<td><%= post.id %></td>
    <td><%= post.date %></td>
    <td><%= post.user.full_name %></td>
    <td><%= truncate(post.rational) %></td>
    <%#= link_to "Edit", edit_post_path(post), id:"edit_#{post.id}" %>
    <%#= link_to "Edit", edit_post_path(post), id: post.id %>
    <td><%= link_to "Edit", edit_post_path(post) %></td>
  </tr>
<% end %>
