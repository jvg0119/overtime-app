<div class="logo">
	<h1>Time Tracker</h1>
</div>

<ul class="custom-nav nav nav-tabs">

	<% if current_user %>
	
		<li class="<%= active?(root_path)%>">	
			<%= link_to "", root_path, class: "glyphicon glyphicon-home" %>
		</li>
	

		<%# if current_user_admin?(current_user) %> <!-- use the policy instead -->
		<!-- move to be rendered by a partial see below:
		<%# if policy(AuditLog).index? %>
			<li class="<%#= active?(audit_logs_path)%>">	
				<%#= link_to "Audit Log", audit_logs_path %>	
			</li>
		<%# end %>
		-->
		<%= render partial: "shared/employee_nav_items" if employee? %>

		<%#= render partial: "shared/audit_log_tab" if policy(AuditLog).index? %>
		<%= render partial: "shared/audit_log_tab" if admin? %>

	<% end %>

	<li class="dropdown pull-right">
		<a href="#" data-toggle="dropdown" class="dropdown-toggle">Options<strong class="caret"></strong></a>
			<ul class="dropdown-menu">
				<%#= link_to "Edit Detail", edit_user_registration_path %>
				
				<%= render 'shared/admin_dropdown' if admin? %>

				<li><%= link_to "Log out", destroy_user_session_path, method: :delete  %></li>
			</ul>
	</li>
</ul>

